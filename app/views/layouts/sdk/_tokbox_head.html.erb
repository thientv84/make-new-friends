<script src="<%= @room[:config]["api_javascript_sdk"] %>" type="text/javascript" charset="utf-8"></script>

<script type="text/javascript">
	var opentok = eval( { session_id: '<%= @room[:config]["api_javascript_sdk"] %>'} );

	var apiKey = <%= @room[:config]["api_key"] %>; 
	var sessionId = '<%= @room[:sessionId] %>'; 
	var token = '<%= @room[:token] %>';

	var session;
	var publisher;
	var subscribers = {};
	
	var sutalkLink = "<%= @room[:sutalkLink] %>"
	
	
	// Un-comment either of the following to set automatic logging and exception handling.
	// See the exceptionHandler() method below.
	// TB.setLogLevel(TB.DEBUG);
	// TB.addEventListener("exception", exceptionHandler);
	
</script>